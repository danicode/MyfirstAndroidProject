<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/android_week3"
    tools:context=".MainActivity"
    android:orientation="vertical"
    android:id="@+id/lyt_activity_main">

    <TextView
        android:id="@+id/lbl_welcome"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/roboto_bold"
        android:gravity="center_horizontal|center_vertical"
        android:text="@string/lbl_welcome_message"
        android:textColor="@color/white"
        android:textSize="@dimen/sizeTitle"
        android:layout_marginTop="@dimen/layoutMarginTop"/>

    <Button
        android:id="@+id/btn_enter"
        android:layout_width="@dimen/widthButtonEnter"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@drawable/border_button"
        android:fontFamily="@font/roboto"
        android:text="@string/btn_enter"
        android:textAllCaps="false"
        android:textColor="@color/white" />

</RelativeLayout>